# 部署铁小微介绍站到 GitHub Pages + 绑定根域 txwrmtzx.top

## 现状:本地已全部改完并验证(工作区未提交)

1. **修复 main 构建中断**(不改这个,部署必失败):`normalize` 抽到独立模块 `data/activityNormalize.js`,`HistoryToday.vue` / `ActivityTimeline.vue` 改为正确导入(VitePress `.data.js` 被组件导入时仅暴露 `data`)
2. **恢复 `.github/workflows/deploy.yml`**:构建去掉 `--base /tiexiaowei-site/`(根域直出),pnpm 11 + node 22,完整 git 历史(GitChangelog 需要)
3. **`docs/public/CNAME`**(内容仅 `txwrmtzx.top`)+ **`docs/public/.nojekyll`**
4. **`config.mts`** sitemap.hostname → `https://txwrmtzx.top`
5. **撤下公开站 `/admin/` 暴露**(任务红线):首页「后台管理」卡片移除(网格 cols-3→cols-2)、about 页文案、维护者指南直链、`appLinks.ts` 移除 admin;保留 member/register
6. README / 交接手册同步部署现状
7. 本地 `pnpm build` + `pnpm audit` 已通过;dist 已验证:CNAME/.nojekyll 落地、sitemap 为新域、无 admin 残留、无旧域名残留

## 批准后的执行步骤

1. 两个 commit(`fix:` 构建修复 + `chore(pages):` 部署配置)→ `git push origin main`
2. `gh api` 创建 Pages 站点(`build_type=workflow`,即 Settings→Pages→Source: GitHub Actions)
3. 等 Actions 跑完(约 2-4 分钟),curl 验证默认地址 `https://lvsugars.github.io/tiexiaowei-site/` 首页/CSS/JS/favicon 全部 200
4. **浏览器实测默认地址**:桌面 + 390×844 + Console 无报错(赶在绑定自定义域前做,避免重定向干扰)
5. `gh api` 绑定自定义域名 `txwrmtzx.top`(HTTPS 证书签发与 Enforce HTTPS 依赖 DNS 先生效,如实标注待办)
6. 二次小改动 push(交接手册补「当前部署信息」小节)→ 用 deployments API 核对新 commit SHA,证明 push→自动构建→发布链路真实可用
7. 只读 curl 验证 njrts 五个入口(`/`、`/member/`、`/admin/`、`/register/`、`/api/health` 必须仍是业务 JSON)
8. WebFetch 核对 GitHub 官方自定义域 DNS 指引(不凭记忆写 IP)
9. 输出任务要求格式的最终报告

## 需要你手动做的:Cloudflare 加 DNS(我无 API token)

根域 A ×4:`185.199.108.153 / 185.199.109.153 / 185.199.110.153 / 185.199.111.153`(TTL 自动,**关闭代理/灰云**,否则 GitHub 证书签发可能失败);可选 AAAA ×4 与 `www` CNAME → `lvsugars.github.io`;`njrts` 的记录一律不碰。执行前我会先按官方文档核对这组 IP。

## 边界

不碰 Tunnel/Docker/cloudflared 目录/任何 credentials;不提交 node_modules/dist/密钥;依赖有 4 条 dev 依赖漏洞告警,按仓库铁律升级需单独 PR,不阻塞本次部署。